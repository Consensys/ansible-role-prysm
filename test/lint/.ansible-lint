---
exclude_paths:
  - .venv/
  - venv/

use_default_rules: true

# ansible-lint v26 uses rule IDs/names instead of legacy numeric codes
skip_list:
  - lines-too-long
  - risky-shell-pipe
  - name[casing]
  - no-handler
  - command-instead-of-shell
  - no-changed-when
  - yaml[line-length]